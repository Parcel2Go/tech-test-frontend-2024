import { Box, Typography } from '@mui/material';

export default function Header() {
  return (
    <Box component="header" padding={8}>
      <Typography component="h1" variant="h3">
        Parcel2Go Technical Evaluation
      </Typography>
    </Box>
  );
}
